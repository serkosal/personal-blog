add celery's tasks for avatar's image processing
add email sending & related tasks

update readme files:
  - environment variables for dev, prod
  - launching dev test stand
  - production deploying
  - architecture
  - project structure

add docstrings
add tests
add ci/cd pipelines

add slug field to Post model
add nofollow relation to create, update, delete links
add blog post caching